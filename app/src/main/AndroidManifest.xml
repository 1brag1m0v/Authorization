<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
    Этот элемент <uses-permission> в манифесте Android приложения запрашивает разрешение
    WRITE_EXTERNAL_STORAGE. Разрешение необходимо для возможности записи данных во внешнее
    хранилище устройства.

    Примечание: Использование этого разрешения требуется в случае, если приложение
    планирует сохранять или читать файлы на внешнем хранилище устройства. Однако,
    начиная с Android 10 (API уровень 29), Google внедрил изменения в системе
    прав доступа к файловой системе, и использование этого разрешения стало менее
    предпочтительным. Вместо этого, рекомендуется использовать внутреннее хранилище
    приложения или использовать FileProvider для обмена файлами с другими приложениями.
-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Authorization"
        tools:targetApi="31">
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".RegActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>